import React from 'react';

const Blog = () => {
    return (
        <div>
            
            <div>
                <h2>what is a perpase react router</h2>
                <p>
                React Router is the standard routing
                 library for React. React Router keeps your UI in 
                  sync with the URL
                </p>
            </div>
          
            
        </div>
    );
};

export default Blog;